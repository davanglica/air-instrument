Python 11 needed. Version further than that doesn't support openCV.
Install all required library in "requirements.txt" by pip -r install requirements.txt.
ffmpeg needed, install by sudo apt-get install ffmpeg
